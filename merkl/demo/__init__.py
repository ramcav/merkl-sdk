"""``merkl.demo`` — the five scenarios, end to end, as pages a stranger can open.

Everything here is wired the way a real deployment is (:mod:`merkl.demo.rig`),
run against a rail (:mod:`merkl.demo.scenarios`), and rendered to a folder that
both verifiers check (:mod:`merkl.demo.pages`). ``merkl demo`` is the CLI entry
point; see ``merkl demo --help``.
"""
